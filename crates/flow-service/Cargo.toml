lints.workspace = true

[package]
name = "netcalyx-flow-service"
version = "0.2.0"
edition = "2024"
authors = ["NetCalyx Authors"]
license = "Apache-2.0"
readme = "README.md"
repository = "https://github.com/network-analytics/NetCalyx"
homepage = "https://github.com/network-analytics/NetCalyx"
description = """
IPFIX/Netflow service
"""
keywords = ["ipfix", "netflow", "parser", "protocol"]
categories = ["network-programming", "parsing"]

[dependencies]
netcalyx-flow-pkt = { workspace = true, features = ["codec"] }
tokio = { workspace = true, default-features = false, features = ["macros", "rt", "time"] }
tokio-util = { workspace = true, default-features = false, features = ["net"] }
bytes = { workspace = true }
serde = { workspace = true, features = ["derive"] }
tracing = { workspace = true }
futures = { workspace = true }
futures-util = { workspace = true }
socket2 = { workspace = true, features = ["all"] }
strum_macros = { workspace = true }
async-channel = { workspace = true }
libc = { workspace = true }
opentelemetry = { workspace = true, features = ["metrics", "trace", "logs"] }
either = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, default-features = false, features = ["macros", "rt", "time", "rt-multi-thread", "signal"] }
chrono = { workspace = true, default-features = false, features = [
    "std",
    "clock",
] }
tracing-subscriber = { workspace = true }
tracing-test = { workspace = true }
serde_json = { workspace = true }
