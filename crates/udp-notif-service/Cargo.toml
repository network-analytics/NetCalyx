lints.workspace = true

[package]
name = "netcalyx-udp-notif-service"
version = "0.2.0"
edition = "2024"
authors = ["NetCalyx Authors"]
license = "Apache-2.0"
readme = "README.md"
repository = "https://github.com/network-analytics/NetCalyx"
homepage = "https://github.com/network-analytics/NetCalyx"
description = """
UDP-Notif service
"""
keywords = ["udp-notif", "parser", "protocol"]
categories = ["network-programming", "parsing"]

[dependencies]
async-channel = { workspace = true }
bytes = { workspace = true }
futures = { workspace = true }
futures-util = { workspace = true }
libc = { workspace = true }
netcalyx-parse-utils = { workspace = true }
netcalyx-udp-notif-pkt = { workspace = true, features = ["codec"] }
serde = { workspace = true, features = ["derive"] }
socket2 = { workspace = true }
strum_macros = { workspace = true }
tokio = { workspace = true, default-features = false, features = ["macros", "time", "net", "signal", "rt-multi-thread"] }
tokio-util = { workspace = true, default-features = false, features = ["net"] }
tracing = { workspace = true }
opentelemetry = { workspace = true, features = ["metrics", "trace", "logs"] }
either = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, default-features = false, features = ["macros", "rt-multi-thread"] }
tracing-subscriber = { workspace = true }
tracing-test = { workspace = true }
serde_json = { workspace = true }
